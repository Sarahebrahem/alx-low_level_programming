#include "main.h"
#include <stdio.h>
/**
 * add - function that adds two integers and returns the result
 * @a: the int to be check
 * @b: the int to be checked
 * Return: result
 */
int add(int a, int b)
{
int result = a + b;
return (result);
}
